import * as React from 'react';
import { LoginForm } from '../login-form/LoginForm';

export const Login: React.FC = () => {
  return (
    <div>
      <h1>YouLift</h1>
      <LoginForm />
    </div>
  );
};
